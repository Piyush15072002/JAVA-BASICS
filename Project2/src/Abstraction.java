// To perform abstraction, we make abstraction class which must have at least one abstraction functions
// using the abstraction keyword

abstract class Bank {

    // these functions and properties are like Blueprints so we don't define them
    // and just delcare them
    String id;

    abstract public void withdrawMoney();

}

public class Abstraction {
    public static void main(String[] args) {

    }
}
